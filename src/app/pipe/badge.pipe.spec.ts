/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BadgePipe } from './badge.pipe';

describe('Pipe: Badgee', () => {
  it('create an instance', () => {
    let pipe = new BadgePipe();
    expect(pipe).toBeTruthy();
  });
});
